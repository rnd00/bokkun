<%= render 'shared/side-bar' if current_user.manager %>
<div class="container <%=raw 'employer-content' if current_user.manager %>">
  <h1 style="text-align:center;">Edit a Receipt</h1>
  <%= render 'form', receipt: @receipt, trip: @trip%>
</div>
