<% current_trip.trip_budgets.each do |trip_budget| %>
  <p><%= trip_budget.budget.name %></p>
  <% if trip_budget.receipts.empty? %>
    <p>No receipts entered.</p>
  <% else %>
    <% trip_budget.receipts.each do |receipt| %>
      <p><%= receipt.company %></p>
    <% end %>
  <% end %>
<% end %>
